# Completion for custom config-updates fish function

complete -c config-update -a asdf -f -d "Update asdf config"
complete -c config-update -a brew -f -d "Update brew config"
complete -c config-update -a fish -f -d "Update fish config"
